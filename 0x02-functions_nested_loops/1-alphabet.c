#include "main.h"
/**
 * print_alphabet - Prints the alphabet in lowercase.
 *
 * Return: always 0 (Success)
 */
void print_alphabet(void)
{
	int i;

	for (n = 'a'; n <= 'z'; n++)
	{
		_putchar(i);
	}

	_putchar('\n');
}
